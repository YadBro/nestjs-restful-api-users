import { ValidateCreateUserPipePipe } from './validate-create-user-pipe.pipe';

describe('ValidateCreateUserPipePipe', () => {
  it('should be defined', () => {
    expect(new ValidateCreateUserPipePipe()).toBeDefined();
  });
});
